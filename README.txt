1. script-threadkeys.txtにスクリプトが出没したスレのスレッドキー（スレURLの最後の部分）を羅列する。
　（スレッドキーの後の文字列（スレタイ）は無視されるので無くてもいいです。
　　get_script_threadkeys.exeで自動的に直近のスクリプトが出没したと思われるスレが書き込まれます。）
2. script_res_selector.exeを起動してスクリプトではないと思われるレスを選択して「OK」を押す。
3. script-res-db.txtに生成したレス一覧が書き込まれます。